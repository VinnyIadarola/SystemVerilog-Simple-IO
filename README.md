A simple way to use high level languages (cpp, python, java) to verify SystemVerilog projects. It uses txt files to act as an interface between the two languages to allow for simplistic debugging. Use the sv_comm's sig_interfaces to hook up to you dut then use the grab and write to display results. 

A better Readme and CPP example will added soon
